<?xml version="1.0" encoding="utf-8"?>
<!-- Reuse the generated ribbon artwork. Inset the plate so the central mark
     clears the 66dp safe circle of the 108dp adaptive canvas. -->
<inset xmlns:android="http://schemas.android.com/apk/res/android"
    android:inset="12dp">
    <bitmap android:src="@drawable/ic_launcher_art"
        android:gravity="fill" android:filter="true" />
</inset>
